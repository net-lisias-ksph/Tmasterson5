PART
{
	name = sylverVLS
	module = Part
	author = TMasterson5
	mesh = model.mu
	rescaleFactor = .7
	
	node_attach = 0.0, -0.000000000001, 0, 0, -1, 0, 0
	node_stack_bottom = 0.0, -0.01, 0.0, 0, -1, 0, 1	

	NODE
	{
		name = cell1
		transform = cell1 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell2
		transform = cell2 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell3
		transform = cell3 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell4
		transform = cell4 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell5
		transform = cell5 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell6
		transform = cell6 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell
		transform = cell7 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE

	NODE
	{
		name = cell8
		transform = cell8 // Or whatever your transform name is.
		size = 1.0 // Will always be 0 until fixed.
		method = FIXED_JOINT // will always be FIXED_JOINT until fixed.
	}
	NODE
	
	stackSymmetry = 1	
	
	TechRequired = start
	entryCost = 0
	cost = 5000
	category = none
	subcategory = 0
	title = Sylver VLS
	manufacturer = DCNS
	description = The Sylver (SYstème de Lancement VERtical) is a vertical launching system (VLS) designed by DCNS. The launcher comes in several variants, each distinguished by their height. A-35 and A-43 were developed for launching short range surface-to-air missiles, the A-50 for the long-range PAAMS air defense system, and the A-70 launcher for larger missiles such as the SCALP Naval land attack cruise missile. The numbers refer to the approximate length of the missile which can be accommodated, in decimetres, i.e. the A-43 can hold missiles which are up to 4.3 metres long whilst the A-70 can accommodate missiles up to 7 metres long. The launchers come in eight-cell modules, except A-35 available in four-cell modules, with each eight-cell module occupying six square metres of deck space.
	attachRules = 1,1,1,1,1
	mass = 2
	dragModelType = default
	maximum_drag = 0.2
	minimum_drag = 0.2
	angularDrag = 1
	crashTolerance = 60
	maxTemp = 3000
	tags = cck-TM5A

 
	{
		name = ModuleTurret
		
		turretID = 0
		
		pitchTransformName = pitchTransform
		yawTransformName = yawTransform
		
		pitchSpeedDPS = 0
		yawSpeedDPS = 0
		
		maxPitch = 0
		minPitch = 0
		
		yawRange = 0
		
		smoothRotation = false
		smoothMultiplier = 10
		
		//audioPath = BDArmory/Sounds/hydraulicLoop
		//maxAudioPitch = 0.42
		//minAudioPitch = 0.15
	}	
	
 
	{
		name = MissileTurret
		
		turretID = 0
		
		finalTransformName = pitchTransform
		deployAnimationName = deploy
		deployAnimationSpeed = 1
		railLength = 8
		firePauseTime = 1.5
		activeMissileOnly = True
	}	
	
 
	{
		 
	}	
}